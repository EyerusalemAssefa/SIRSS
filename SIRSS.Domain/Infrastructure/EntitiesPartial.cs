namespace SIRSS.Domain.Infrastructure
{
    /* Here you don't have to put any thing but the class name must be the same as PETAEntities's
      and also they have to be partial. Then Use the MetaDataType attribute and call the MetaData
      class which is appropriate for that class.*/

    //[MetadataType(typeof(UserLoginMetaData))]
    //public partial class UserLogin
    //{
    //}
    /* You can add more*/
}